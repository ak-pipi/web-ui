# Vue 管理后台前端开发计划

> 基于 `new_rules` v2.3 需求文档，按 Vue 管理后台前端（web_ui/）的功能边界拆分。本文档仅涵盖 **web_ui/** 的开发任务。

---

## 一、现有基础

当前 web_ui 基于 RuoYi-Vue 前端框架（Vue 2 + Element UI），已实现：登录、动态路由与权限、用户/角色/菜单/部门/字典/配置管理、定时任务、操作日志、在线用户、服务器监控、游戏房间查询（麻将/比鸡/逮狗腿/百人牛牛）、玩家管理。

---

## 二、经营驾驶舱（首页重构）

替换当前首页为经营驾驶舱。

### 核心指标卡片

| 指标 | 说明 |
|---|---|
| 今日新增用户 | 当日注册玩家数 |
| 今日活跃用户 | 当日登录或游戏玩家数 |
| 当前在线人数 | 实时在线玩家 |
| 当前在线房间 | 正在进行的房间 |
| 今日开房数 | 创建房间数量 |
| 今日完成局数 | 已完成游戏局数 |
| 今日房卡消耗 | 房卡消耗量 |
| 今日积分流转 | 游戏积分变动总量 |
| 异常牌局数 | 风控标记房间 |
| 客服待处理 | 未处理工单 |

### 图表（ECharts）

1. 在线人数趋势
2. 游戏局数趋势
3. 房卡消耗趋势
4. 各游戏占比（饼图）
5. 新增用户来源
6. 异常风险趋势
7. 活动奖励成本

---

## 三、游戏管理页面

### 3.1 游戏列表页

路由：`/niuma/game/list`

表格字段：游戏 ID、游戏名称、游戏类型（麻将/扑克/字牌）、状态（上架/下架/维护）、支持人数、当前在线、今日局数、今日房费、版本号、更新时间

操作按钮：新增、编辑、上架、下架、维护、配置玩法、查看房间、查看报表、灰度发布、回滚版本

### 3.2 每款游戏的规则配置页

为 6 款新游戏各建独立规则配置页面：

#### 桃江麻将规则配置（`/niuma/game/taojiang-mahjong/rules`）

表单字段：
- 游戏状态、人数、局数（4/8/16）
- 底分（1/2/5）、单局封顶
- 房费方式（房主支付/AA/大赢家支付）
- 是否允许碰、杠、吃、自摸、点炮
- 是否启用赖子、红中
- 庄家规则（轮庄/赢者坐庄/房主首庄）
- 是否允许投票解散

同时提供 JSON 高级配置模式，可切换表单/JSON 视图。

#### 红中麻将规则配置（`/niuma/game/hongzhong-mahjong/rules`）

字段：红中数量、是否作为赖子、赖子替代规则、是否允许红中参与胡牌、七对、碰碰胡、杠分、自摸加倍

新增：**胡牌算法测试器** — 输入手牌 + 赖子数量 + 规则配置，调用后台接口输出是否可胡、牌型、番数、得分

#### 跑得快规则配置（`/niuma/game/paodekuai/rules`）

字段：允许牌型、炸弹规则、首出规则（是否必须带黑桃3）、过牌规则、托管时间、封顶分

#### 长沙麻将规则配置（`/niuma/game/changsha-mahjong/rules`）

字段：起手胡开关（7 种各独立开关）、中鸟数量、鸟分规则、番型开关、封顶番数、房费方式

新增：**起手胡牌型测试器** — 输入 14 张牌，返回可触发的起手胡类型

#### 益阳歪胡子规则配置（`/niuma/game/yiyang-waihuzi/rules`）

字段：最低胡息、提/跑/偎胡息、特殊牌型、胡息转分比例、封顶分

#### 沅江千分规则配置（`/niuma/game/yuanjiang-qianfen/rules`）

字段：目标分、计分牌配置（5/10/K 分值）、叫分规则、庄家规则、炸弹规则、逃跑惩罚、封顶

### 3.3 规则版本管理

每个规则配置页支持：
- 保存草稿
- 提交审批
- 查看历史版本
- 回滚到旧版本
- 灰度发布控制（按百分比、按渠道）

---

## 四、房间管理页面

### 4.1 房间列表页（`/niuma/room/list`）

筛选条件：游戏类型、房间状态、时间范围、房间号

表格字段：房间号、游戏名称、玩法版本、房主、人数（当前/最大）、局数（当前/总）、状态、房费、创建时间、结束时间、风险标记

### 4.2 房间详情页（`/niuma/room/detail/{id}`）

标签页结构：
1. **基础信息** — 房间配置快照
2. **玩家列表** — 参与玩家、座位、积分变化
3. **每局结算** — 局号、结算时间、玩家得分、胡牌信息
4. **积分流水** — 该房间关联的积分变动
5. **操作日志** — 每步操作记录
6. **网络日志** — 连接、断线、重连记录
7. **回放** — 内嵌回放播放器或链接
8. **风控标记** — 异常标记、争议标记

操作：强制解散、标记争议、导出数据

---

## 五、玩家管理页面增强

### 5.1 玩家列表（`/niuma/player/list`）

新增列：娱乐积分、房卡、保险箱余额、今日局数、胜率、风险等级

### 5.2 玩家详情页（`/niuma/player/detail/{id}`）

标签页结构：
1. **基本信息** — 昵称、头像、手机号（脱敏）、注册时间、最近登录
2. **设备信息** — 设备型号、系统版本
3. **IP 记录** — 登录 IP 列表
4. **游戏战绩** — 各游戏局数、胜率统计
5. **积分流水** — 筛选类型、时间
6. **房费流水**
7. **奖励流水**
8. **保险箱流水**
9. **客服工单** — 关联的工单列表
10. **风控标签** — 风险等级、风控备注
11. **后台操作记录** — 对该玩家的所有后台操作

操作：冻结、解冻、封禁、添加风控备注

---

## 六、风控中心页面

### 6.1 风控总览（`/niuma/risk/overview`）

- 各类风控规则命中数统计
- 异常玩家列表
- 最近风控事件

### 6.2 异常检测列表（`/niuma/risk/detection`）

筛选：风控类型（同IP多号/同设备多号/固定同桌/固定输赢/异常胜率/异常局数/异常逃跑/异地登录）

表格：玩家 ID、昵称、风险等级、检测类型、详情、检测时间、处理状态

### 6.3 风控处理

操作按钮：
- 标记观察
- 限制匹配
- 冻结账号
- 冻结保险箱
- 禁止创建房间
- 强制下线
- 转客服复核
- 提交审计

---

## 七、牌局审计与回放页面

### 7.1 牌局查询（`/niuma/audit/rounds`）

筛选：房间号、玩家 ID、游戏类型、时间范围、异常标记

表格字段：房间号、局号、游戏类型、玩家、胡牌玩家、结算时间、异常标记

### 7.2 回放页面（`/niuma/audit/replay/{round_id}`）

功能：
- 时间轴展示每步操作
- 播放/暂停/快进
- 查看每一步状态（手牌、桌面、操作）
- 导出争议证据包

### 7.3 各游戏专属审计页

- 桃江麻将：异常检测（频繁同桌、固定输赢、同 IP 多号）
- 红中麻将：红中使用次数、赖子替代记录、胡牌路径、非法胡牌核查
- 跑得快：出牌日志（轮次、玩家、出牌内容、牌型、压牌、剩余牌数、耗时）、托管与逃跑统计
- 长沙麻将：起手胡统计、中鸟审计
- 歪胡子：胡息明细审计、吃牌路径复盘
- 沅江千分：叫分记录、得分明细

---

## 八、活动奖励管理页面（`/niuma/activity/list`）

- 活动列表（活动名称、奖励类型、奖励数量、时间、状态）
- 活动编辑/新增（表单 + 规则配置）
- 活动数据统计

---

## 九、保险箱管理页面（`/niuma/safebox/list`）

- 按玩家查询保险箱余额
- 保险箱流水查询
- 异常存取记录查看

---

## 十、推广渠道管理页面（`/niuma/channel/list`）

- 渠道码生成、邀请链接管理
- 渠道数据：新增用户、活跃用户、房卡消耗
- 奖励发放记录
- 异常渠道封禁

---

## 十一、客服工单页面（`/niuma/support/ticket/list`）

### 工单列表

筛选：工单类型、处理状态、时间范围

字段：工单号、玩家、类型、状态、客服、创建时间

### 工单处理页

- 玩家信息面板
- 相关房间/流水查看
- 处理意见填写
- 转风控/财务审批
- 回复玩家
- 关闭工单

---

## 十二、权限角色管理增强

### 角色配置

| 角色 | 权限范围 |
|---|---|
| 超级管理员 | 全部权限 |
| 运营主管 | 游戏配置、活动、公告、报表 |
| 普通运营 | 查询、活动执行、公告草稿 |
| 财务 | 对账、补偿审批 |
| 客服 | 工单、玩家查询、基础处理 |
| 风控 | 风控中心、牌局审计、账号冻结 |
| 技术运维 | 系统监控、发布、日志 |

按钮级权限控制，重要操作二次确认弹窗。

---

## 十三、审计日志页面（`/monitor/audit-log`）

增强现有操作日志：

字段：操作人、操作时间、操作 IP、操作模块、操作前数据（JSON）、操作后数据（JSON）、操作原因

支持：按模块、操作人、时间范围筛选

---

## 十四、版本升级管理页面

### 14.1 App 版本列表（`/niuma/version/app`）

字段：版本号、平台、渠道、更新类型、安装包地址、包体大小、文件校验、最低兼容版本、更新文案、状态、灰度比例

### 14.2 热更新包管理（`/niuma/version/hotfix`）

字段：热更新版本、适用 App 版本、适用游戏、更新内容、资源包地址、文件校验、是否强制、灰度比例、回滚版本

### 14.3 Bug 修复记录（`/niuma/version/bug`）

字段：Bug 编号、影响版本、影响游戏、严重等级（P0/P1/P2/P3）、问题描述、修复方案、测试结果、发布版本、回滚预案

### 14.4 灰度与回滚控制

- 按用户 ID / 设备系统 / 渠道 / 地区 / 百分比灰度
- 一键暂停更新
- 一键回滚
- 更新成功率/失败率/崩溃率查看

---

## 十五、音效与特效配置中心页面

### 15.1 音效配置（`/niuma/resource/audio`）

字段：游戏名称、场景、事件编码、音效文件、默认音量、是否循环、是否启用、资源版本

操作：上传音效、绑定事件、灰度发布、回滚、一键关闭

### 15.2 特效配置（`/niuma/resource/vfx`）

字段：特效编码、绑定事件、特效资源、默认等级（L1-L5）、粒子上限（低/中/高端）、是否全屏、是否可跳过、最低机型要求、灰度比例

操作：上传特效、按游戏绑定事件、灰度发布、回滚、一键关闭

### 15.3 资源统计

- 资源加载失败统计
- 特效导致的崩溃率
- 低端机默认特效等级配置

---

## 十六、系统配置页面

新增配置项：
- 音效资源版本
- 特效资源版本
- 低端机默认特效等级
- 金币粒子密度
- 强制更新最低版本
- 客服入口配置
- 公告配置

---

## 十七、开发阶段

### 第一阶段：核心页面

1. 经营驾驶舱（首页重构）
2. 游戏列表管理页
3. 6 款游戏规则配置页（表单 + JSON 双模式）
4. 规则版本管理
5. 房间管理页（列表 + 详情）

### 第二阶段：运营管理

1. 玩家管理增强（详情页标签页结构）
2. 积分流水/房费流水/奖励流水/保险箱流水页面
3. 活动奖励管理
4. 推广渠道管理
5. 客服工单系统

### 第三阶段：风控与审计

1. 风控中心（总览 + 检测列表 + 处理操作）
2. 牌局审计与回放页面
3. 各游戏专属审计页
4. 审计日志增强
5. 权限角色细化

### 第四阶段：版本与资源管理

1. 版本升级管理（App 版本 + 热更新 + Bug 追踪）
2. 灰度与回滚控制面板
3. 音效配置中心
4. 特效配置中心
5. 资源统计页面

---

## 十八、UI 设计规范

### 页面风格

- 左侧深色导航栏
- 顶部显示登录管理员、消息、环境标识
- 主区域白底卡片
- 表格支持筛选、搜索、导出
- 重要操作二次确认弹窗
- 风险数据使用标签展示（红/橙/黄）
- 财务数据需要权限提示
- 规则配置页使用表单 + JSON 高级配置双模式
- 回放页要有时间轴和操作步骤列表

### 新增路由结构

```
/niuma/
  game/
    list                          -- 游戏列表
    taojiang-mahjong/rules       -- 桃江麻将规则
    hongzhong-mahjong/rules      -- 红中麻将规则
    paodekuai/rules              -- 跑得快规则
    changsha-mahjong/rules       -- 长沙麻将规则
    yiyang-waihuzi/rules         -- 歪胡子规则
    yuanjiang-qianfen/rules      -- 沅江千分规则
  room/
    list                          -- 房间列表
    detail/:id                    -- 房间详情
  player/
    list                          -- 玩家列表
    detail/:id                    -- 玩家详情
  risk/
    overview                      -- 风控总览
    detection                     -- 异常检测
  audit/
    rounds                        -- 牌局查询
    replay/:roundId               -- 回放
  activity/
    list                          -- 活动列表
  safebox/
    list                          -- 保险箱管理
  channel/
    list                          -- 推广渠道
  support/
    ticket/list                   -- 客服工单
  version/
    app                           -- App 版本
    hotfix                        -- 热更新
    bug                           -- Bug 追踪
  resource/
    audio                         -- 音效配置
    vfx                           -- 特效配置
```

---

## 十九、验收标准

1. 每款游戏有独立规则配置页（表单 + JSON 双模式）
2. 规则修改支持版本、审批、灰度、回滚
3. 房间详情包含完整的玩家、结算、流水、操作日志、回放
4. 玩家详情包含 11 个标签页信息
5. 风控中心能展示各类异常检测结果并支持处理操作
6. 回放页面有时间轴和步骤列表
7. 权限细分到按钮级，7 种角色各有限定权限
8. 所有后台操作有审计日志（含操作前后数据对比）
9. 版本管理支持灰度发布和一键回滚
10. 音效特效配置中心支持上传、绑定、灰度、回滚、一键关闭
